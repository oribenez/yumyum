import classes from './Header.module.css';
import HeaderBar from './HeaderBar';
import HeaderWelcomeImg from './HeaderWelcomeImg';

const Header = (props) => {
	return (
		// <header className={classes.headerWrap}>
		// 	<HeaderBar />
		// </header>
	);
};

export default Header;
