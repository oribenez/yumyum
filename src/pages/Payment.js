// eslint-disable-next-line
import classes from './Payment.module.css';
import Story from '../Components/UI/Story';

const Payment = (props) => {
	return (
		<>
			<Story title="Payment"></Story>
		</>
	);
};

export default Payment;
